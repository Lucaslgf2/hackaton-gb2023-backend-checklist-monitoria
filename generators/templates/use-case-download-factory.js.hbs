import { CreateFile{{pascalCase name}} } from '@/data/use-cases/{{dashCase name}}/create-file-{{dashCase name}}'
import { ICreateFile{{pascalCase name}} } from '@/domain/protocols/{{dashCase name}}/{{dashCase name}}-protocol'
import { makeAwsS3Adapter } from '@/main/factories/adapters/aws-s3-adapter-factory'
import { makeExcelJsAdapter } from '@/main/factories/adapters/excel-js-adapter-factory'
import { makeGenerateFileName } from '@/main/factories/use-cases/utils/generate-file-name-factory'
import { makeFind{{pascalCase name}} } from './find-{{dashCase name}}-factory'

export const makeCreateFile{{pascalCase name}} = (): ICreateFile{{pascalCase name}} => {
  return new CreateFile{{pascalCase name}}(makeFind{{pascalCase name}}(), makeExcelJsAdapter(), makeGenerateFileName(), makeAwsS3Adapter())
}
