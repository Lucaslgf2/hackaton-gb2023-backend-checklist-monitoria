import { adaptServerlessRoute, IHandlerContext, IHandlerEvent } from '@/main/adapters/serverless/serverless-route-adapter'
import { loadAwsSecrets } from '@/main/config/load-aws-secrets'
import { make{{pascalCase verb}}{{pascalCase name}}Controller } from '@/main/factories/controllers/{{dashCase name}}//{{dashCase verb}}-{{dashCase name}}-controller-factory'
import { IHttpResponse } from '@/presentation/protocols'

export const {{dashCase verb}}{{pascalCase name}}Handler = async (event: IHandlerEvent, context: IHandlerContext): Promise<IHttpResponse> => {
  await loadAwsSecrets()
  const controller = make{{pascalCase verb}}{{pascalCase name}}Controller()
  return adaptServerlessRoute(controller)(event, context)
}
